<h1>Edit your link</h1>
<div class="edit-link">
  <%= form_for @link do |f| %>
    <%= f.url_field :url %>
    <%= f.label :url, 'URL' %>

    <%= f.text_area :title %>
    <%= f.label :title %>

    <%= f.submit "Submit" %>
  <% end %>
</div>
